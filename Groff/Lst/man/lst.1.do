#!/bin/sh

# ----------------------------------------------------------------------
# lst.1.do
# ----------------------------------------------------------------------

lst_awk=../src/lst.awk
redo-ifchange $lst_awk
redo-ifchange lst.1.t
awk -f $lst_awk lst.1.t > $3
