#!/bin/sh

# ----------------------------------------------------------------------
# clean.do
# ----------------------------------------------------------------------

for file in `find . -name '*.pdf' -or -name '*.html' -or -name '*.lst.t' -or -name '*~'`
do
  rm $file
done
